% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gradient.R
\name{set_traits}
\alias{set_traits}
\title{Replace the traits on an existing leaf}
\usage{
set_traits(x, traits)
}
\arguments{
\item{x}{a \code{Leaf}, from \code{\link[=leaf_model]{leaf_model()}}}

\item{traits}{a \code{\link[=leaf_traits]{leaf_traits()}} object}
}
\value{
\code{x}, invisibly.
}
\description{
Changes the traits of a \code{Leaf} in place, leaving \code{\link[=leaf_control]{leaf_control()}}'s numerical
settings alone. This is the fast path for anything that varies traits --
calibration, sensitivity analysis, a trait response curve -- because
constructing a \code{Leaf} from R costs about 45 times as much as solving one.
}
\section{You must set the drivers again afterwards}{

This returns the leaf to its just-constructed state, so \code{\link[=set_drivers]{set_drivers()}} has to
be called before the next solve. That is not caution: \code{vcmax_}, \code{jmax_} and
\code{R_d_} are derived from the traits inside \code{set_physiology()}, so they are
genuinely unknown until the drivers are re-supplied.

The reason it is a function rather than fifteen assignable fields is that
\code{leaf$vcmax_25 <- x} could not be made correct. Three separate pieces of
derived state go stale on a bare trait write: the two pre-integrated
vulnerability splines, the solved operating point, and -- least visibly --
\code{vcmax_}/\code{jmax_}/\code{R_d_}, which \code{set_physiology()} computes behind a cache keyed
on leaf temperature and O2 alone. That last one means the obvious repair,
"change the trait and then set the drivers again", silently does not work.
}

\examples{
l <- leaf_model()
set_traits(l, leaf_traits(vcmax_25 = 120))
set_drivers(l, psi_soil = 2.0, PPFD = 900)
l$optimise()
operating_point(l)
}
\seealso{
\code{\link[=leaf_model]{leaf_model()}}, \code{\link[=set_drivers]{set_drivers()}}, \code{\link[=leaf_gradient]{leaf_gradient()}}
}
